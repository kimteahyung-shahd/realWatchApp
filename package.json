{
  "name": "watch-app",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "dev:api": "json-server --watch backend/db.json --port 5000",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
